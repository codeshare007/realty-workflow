import { NgModule } from '@angular/core';
import { SigningDoneRoutingModule } from './signing-done-routing.module';
import { SigningDoneComponent } from './signing-done.component';

@NgModule({
    imports: [
        SigningDoneRoutingModule,
    ],
    declarations: [
        SigningDoneComponent,
    ],
    providers: [
    ]
})
export class SigningDoneModule { }
